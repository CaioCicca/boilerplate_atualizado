import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 20,
        marginBottom: 16,
        fontWeight: 'bold'
    },
});

export default styles;