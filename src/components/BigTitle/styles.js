import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 30,
        marginBottom: 16,
        fontWeight: 'bold'
    },
});

export default styles;